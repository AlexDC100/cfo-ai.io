# Settings + Marketing — round 1 critique (settings only; shots: settings-marketing-r1)

- Hierarchy: serif hero replaced by 11px caps eyebrow → 19px title; caps section titles now rhyme with PanelHeader, so the column scans as stacked panels. Danger zone reads clearly last. PASS.
- Density: 32px inputs + segmented rails tightened the top half; Plan/Learning cards (other components) are still airier than my sections but not jarring. OK.
- Contrast: rates readout mono ink on bg-2 is legible in both themes; caution amber for "offline fallback" is AA on paper; alert tokens carry the danger zone in Terminal without neon red. PASS.
- Soul: FX rates are real numbers through <Amount>, no fake provenance affordance; "display only" caption scopes them. Theme control says Paper/Terminal over light/dark. PASS.
- Consistency: language/currency/theme/density all use one SegmentedControl; animated teal sweep removed from Settings so active = raised segment everywhere. Remaining nit: harness-forced dark shows "Paper" active (screenshot artifact — harness writes the wrong storage key, not a product bug).

Next round: marketing home (ticker cap, reduced motion, proof strip, phosphor accent) + pricing retheme.
