# Settings + Marketing — round 2 critique (FINAL)
Shots: settings-marketing-r2 (settings, both themes), settings-marketing-r2-mkt (+ -fix) for / and /pricing via a second vite on :5199 with test mode off (the :5173 test-mode stack redirects marketing routes to /dashboard).

- Hierarchy: /settings opens caps-eyebrow → 19px title, sections as caps-labeled panels, danger zone last and unmistakable. Landing hero: headline > sub > CTA > product mock, with the ticker demoted to atmosphere. Pricing hero unchanged structurally (retheme only). PASS.
- Density: 32px inputs + one 32px segmented rail for language/currency/theme/density removed three competing pill styles; landing proof strip is three tight mono rows instead of four fake animated bars. PASS.
- Contrast: ticker layer capped at .55 (.32 under 760px) beneath strengthened overlays — F5F5F5 display text sits on effectively near-black, AA+; gradient CTAs flipped to ink-on-phosphor (#05110D on --grad; text-primary-foreground on /pricing) so both themes clear AA; caution amber for stale FX, alert red only in the danger zone. PASS.
- Soul: proof strip shows REAL measured claims (0.00% drift 5/8 exact, 17/17 gates, byte-identical reruns) that mirror the adjacent copy — the decorative Peer A/B/C numbers are gone; FX rates render through <Amount> with no fabricated provenance affordance and a "display only" caption; motion pauses fully under prefers-reduced-motion (CSS drift + JS intervals). PASS.
- Consistency: landing/marketing-header/consent all inherit the phosphor family from the one scoped var block; Settings red/teal one-offs now alert/caution/brand tokens; serif lives only on marketing (Settings' serif hero and the dead SubscriptionCard serif removed). Residual nit: harness-forced dark shows "Paper" active in the Appearance control (the harness writes the wrong storage key — screenshot artifact, verified not a product bug). DONE.
