# CHAT lane — critique r1 (shots: chat-r1, chat-r1-states)

- **Hierarchy** — degraded panel reads first in the thread (calm headline → Retry → quiet details); composer and pills clearly subordinate when locked. Empty state still led by the serif hero (allowed: empty-state exception), but the assistant ANSWER panel is unverified — no shot of a successful reply yet. r2 must seed one.
- **Density** — conversation column airy and legible; history panel is one hairline surface with search + rows, no double borders. The 2-line composer reads as a writing surface, not a search box.
- **Contrast** — user bubble tint holds in both themes (dark: deep teal wash, light: restrained wash); "Details" reason is quiet but readable; the ⌘↵ hint at ink-faint borders on invisible in dark — bump to ink-mute in r2.
- **Soul** — flat hairlines, no resting shadows, one accent (send / new-chat brand fill, grounding chip). The old animated-gradient send button and glass blur are gone. No raw JSON, status codes, or request ids anywhere in the degraded DOM (D8 test green).
- **Consistency** — grounding + no-workspace pills now both instrument Chips (accent / caution); delete hover uses alert tokens; radii on the 6/10/12 scale. Open items for r2: assistant answer panel look, slide-over panel variant spot-check, hint contrast.
