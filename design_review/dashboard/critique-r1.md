# Dashboard lane — critique r1

- Hierarchy: header now PageHeader (eyebrow/title/trust/Export) + caption + source + trust lines — right order, but header chip and accuracy chip show duplicate "Unverified" on the demo state.
- Density: washes retired (accuracy panel, mint demo banner) — big win; recommendation cards still carry card-2026 resting shadows.
- Contrast: KPI row mono via AmountGroup works, but "295.1 MRON" — magnitude suffix collides with the currency code; health score rounds 85.5→86.
- Soul: gauge + trust chips landed; statements still serif-headed with brand rules (not yet touched this round).
- Consistency: metrics grid (cross-lane MetricCard) serif clashes; statement tabs pending.
