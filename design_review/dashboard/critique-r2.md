# Dashboard lane — critique r2

- Hierarchy: fixed score digits (85.5), fixed "M RON" joint via narrow no-break space; header/dark/mobile all read in order.
- Density: rec cards de-shadowed to hairline panels; statement CSS now 32px rows, caps headers, double-rule totals.
- Contrast: risks tab tokens landed (success/caution/alert); P&L ledger rules verified in shot.
- Soul: double hairline under Net profit and CF reconciliation reads as the ledger motif; trust receipt opens under the chip.
- Consistency: BS fallback table still non-mono; export tab still colored sleeves; ?tab= deep links drop via test-mode fallback (pre-existing) — pinned ?period=demo-meridian for shots.
