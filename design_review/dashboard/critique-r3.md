# Dashboard lane — critique r3

- Hierarchy: statement tabs verified (P&L, BS, CF, risks) — caps section headers, sticky under the 56px header.
- Density: cash-flow empty demo renders calm; notes empty state honest.
- Contrast: BS fallback now mono with double-rule totals; DCF/growth/credit tables mono numerals.
- Soul: risks tab reads instrument-grade (mono A / 4.34, safe-zone chip in success tint).
- Consistency: export sleeves (green/orange raw palette) replaced with hairline panels; RatioTile still sans figures + hover shadow → fix next round; header double-chip → suppress unknown band.
