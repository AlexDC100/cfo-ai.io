# Dashboard lane — critique r4 (final)

- **Hierarchy:** PASS. Serif hero evicted: caps eyebrow → 19px title → one caption line → mono source line → trust line → sticky hairline tabs → gauge → KPI row → recommendations. The health gauge (mono 85.5 over a hairline track) is the first instrument moment and it reads before anything else; Export is the single header action.
- **Density:** PASS. Header stack went from ~5 painted washes to four one-line meta rows; accuracy/demo/statutory washes are now a chip + hairline lines with tap-open receipts. Statement rows sit at ~32px; nothing double-scrolls on 390px.
- **Contrast:** PASS. One accent only; semantic color survives solely on semantic elements (band-tinted gauge/chips, alert reserved for imbalance/critical). KPI figures are mono `<Amount>` under one `AmountGroup` scale ("295.1 M RON" beside "17.7 M RON", never mixed magnitudes); dark Terminal theme verified — track, chips and tints all read.
- **Soul:** PASS. Ledger motif landed: double hairline over Net profit (P&L), Total assets / Total L+E (BS fallback + canonical view), CF reconciliation; provenance-honest — no fake trust affordances (aggregates render plain, "Unverified" shown when telemetry is absent, header chip suppressed rather than duplicated on unknown).
- **Consistency:** PASS with one flagged cross-lane gap: the "All metrics" grid (components/dashboard/MetricCard, serif figures + shadowed cards) and EbitdaMultiplePrimaryCard's mint gradient are other-lane files rendering on this route — reported, not touched. Everything in-lane (tabs, stat panels, statements, risks, ratios, export panels, credit card, KpiCard/KpiTile) now shares hairline + mono + caps-label chrome.
