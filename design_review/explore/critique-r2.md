# EXPLORE lane — critique r2 (/public-companies, /workspace) — PASS

- Hierarchy: header → underline tabs → search → pulse → grid → benchmarking scans top-down with one accent; wizard reads eyebrow → title → stepper → form with the active step the only filled element. Both routes verified at 1440/1280/390 in both themes.
- Density: card grid at 12px gaps, bench tiles with inset leader/laggard sleeve, 32px hairline inputs in wizard and settings — nothing crowds, nothing floats loose. Mobile shows no horizontal overflow; the data-sources "i" now rides the search row.
- Contrast: movement and severity colors are token-driven (success/alert), AA in both themes; magnitude figures read "120.00 B" with RON declared once in the header chips — the "BRON" artifact is gone.
- Soul: flat hairline panels throughout; no resting shadows outside the floating compare tray/drawer; serif and the animated teal fill are fully retired from both routes; monogram avatars are quiet neutral so the ticker text carries identity.
- Consistency: filter, group and peer chips share one selected treatment (brand-tint + accent text); drawer, risk radar, map, compare sheet all re-verified interactively — flows (drawer deep-link, wizard steps, chip toggles, compare, bench drill) behave exactly as before.
