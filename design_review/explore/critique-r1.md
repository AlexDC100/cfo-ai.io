# EXPLORE lane — critique r1 (/public-companies, /workspace)

- Hierarchy: compact PageHeader + underline tabs + search-first order reads correctly; benchmark tiles now lead with the metric label and one mono figure. Workspace wizard header and stepper are calm and scan in one pass.
- Density: card grid on gap-12px holds; bench tiles' inset leader/laggard sleeve keeps three levels without borders-in-borders. Wizard form is airy but the name input runs full-bleed wide — needs a measured max width.
- Contrast: token sweep landed — movement colors are success/alert in both themes; dark theme legible everywhere checked; monograms neutral so the one accent stays reserved.
- Soul: main failure this round is "40.00 BRON" / "584.0 MRON" — magnitude suffix concatenated with RON reads as a fake currency; drop the inline currency on magnitude-scaled figures (page declares RON once in the header chips). Mobile: the data-sources "i" floats centered on its own row under the search bar.
- Consistency: filter/group chips share one selected treatment; serif is gone from both routes; remaining glow/animated-fill purged from workspace rail, stepper, dropzone and periods buttons.
