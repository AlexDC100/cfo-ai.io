# GOLDEN CORPUS case metadata — see corpus/README.md for the schema.
case_id: "exact_zero"
jurisdiction: "RO"
expected_parser: "saga_10_col"
period: "FY2025"
period_end: "2025-12-31"
synthetic: true
anonymized: false
expect_ai_never_consulted: true
source_notes: "SYNTHETIC adversarial case. balanced source → difference exactly 0.00, status BALANCED, auto-reconcile strict no-op, NO adjustment line anywhere. The file's own totals row faithfully sums the account rows, so extraction stays honest (anchor MATCHED) and any gap is the SOURCE's own (D1)."
