User-agent: Googlebot
Allow: /

User-agent: Bingbot
Allow: /

User-agent: Twitterbot
Allow: /

User-agent: facebookexternalhit
Allow: /

User-agent: *
Allow: /

# Public RO directory sitemap (index of gzip shards, generated by
# scripts/public_seo.py sitemaps; served by the backend at /sitemap.xml
# + /sitemaps/*.xml.gz once the Caddy clean-path matcher is added —
# reachable at /api/public/ro/sitemap.xml before that).
# GOTCHA: public/ is ALSO republished to GitHub Pages by
# .github/workflows/deploy-pages.yml under a repo-path prefix. This
# absolute URL deliberately points at the canonical host so the Pages
# copy never advertises a duplicate sitemap on its own origin.
Sitemap: https://cfo-ai.io/sitemap.xml
