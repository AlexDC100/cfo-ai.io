{
  "_comment": "GENERATED — do not edit by hand. Flattened field->type listing of docs/served_envelope.schema.json, locked by tests/engine/test_envelope_contract.py. Regenerate: python tests/engine/test_envelope_contract.py --regen",
  "envelope_version": "sv1",
  "fields": {
    "$.statements": "object",
    "$.statements.assembled_bs": "ref:assembled_bs_serve",
    "$.statements.assembled_pl": "ref:assembled_pl_serve",
    "$.statements.canonical_bs": "ref:canonical_bs",
    "$defs.assembled_bs_serve": "object",
    "$defs.assembled_bs_serve.bs_balance_delta": "number",
    "$defs.assembled_bs_serve.total_assets": "number",
    "$defs.assembled_bs_serve.total_current_assets": "number",
    "$defs.assembled_bs_serve.total_current_liabilities": "number",
    "$defs.assembled_bs_serve.total_equity": "number",
    "$defs.assembled_bs_serve.total_liabilities": "number",
    "$defs.assembled_bs_serve.total_non_current_assets": "number",
    "$defs.assembled_bs_serve.total_non_current_liabilities": "number",
    "$defs.assembled_pl_serve": "object",
    "$defs.assembled_pl_serve.reconciliation_adjustment": "object",
    "$defs.assembled_pl_serve.reconciliation_adjustment.amount": "number",
    "$defs.assembled_pl_serve.reconciliation_adjustment.label": "string",
    "$defs.assembled_pl_serve.reconciliation_adjustment.placement": "enum:bs,pl_other_expense,pl_other_income",
    "$defs.assembled_pl_serve.reconciliation_adjustment.synthetic": "const:True",
    "$defs.canonical_bs": "object",
    "$defs.canonical_bs.classification": "object",
    "$defs.canonical_bs.classification.method": "enum:deterministic,llm",
    "$defs.canonical_bs.classification.model": "null|string",
    "$defs.canonical_bs.classification.needs_review": "array|null",
    "$defs.canonical_bs.classification.prompt_version": "null|string",
    "$defs.canonical_bs.consensus": "ref:consensus",
    "$defs.canonical_bs.diagnosis": "array",
    "$defs.canonical_bs.diagnosis[]": "object",
    "$defs.canonical_bs.diagnosis[].code": "string",
    "$defs.canonical_bs.diagnosis[].detail": "string",
    "$defs.canonical_bs.diagnosis[].leaf_ids": "array",
    "$defs.canonical_bs.diagnosis[].leaf_ids[]": "string",
    "$defs.canonical_bs.difference": "number",
    "$defs.canonical_bs.envelope_version": "const:sv1",
    "$defs.canonical_bs.excluded": "array",
    "$defs.canonical_bs.extraction": "object",
    "$defs.canonical_bs.extraction.header_row_index": "integer",
    "$defs.canonical_bs.extraction.method": "enum:deterministic,llm,mechanical_mapped",
    "$defs.canonical_bs.extraction.model": "null|string",
    "$defs.canonical_bs.extraction.number_locale": "enum:anglo,ro",
    "$defs.canonical_bs.extraction.parser_version": "string",
    "$defs.canonical_bs.extraction.prompt_version": "null|string",
    "$defs.canonical_bs.extraction.sheet": "null|string",
    "$defs.canonical_bs.extraction.source_format": "string",
    "$defs.canonical_bs.invariants": "object",
    "$defs.canonical_bs.jurisdiction": "null|object|string",
    "$defs.canonical_bs.mapping_version": "string",
    "$defs.canonical_bs.needs_review": "array|boolean|null",
    "$defs.canonical_bs.reconcile_offer": "boolean",
    "$defs.canonical_bs.reconciliation": "ref:reconciliation_receipt",
    "$defs.canonical_bs.reprocessed": "object",
    "$defs.canonical_bs.rows": "array",
    "$defs.canonical_bs.rows[]": "object",
    "$defs.canonical_bs.rows[].account_codes": "array",
    "$defs.canonical_bs.rows[].account_codes[]": "string",
    "$defs.canonical_bs.rows[].amount": "number",
    "$defs.canonical_bs.rows[].id": "string",
    "$defs.canonical_bs.rows[].label": "string",
    "$defs.canonical_bs.rows[].label_key": "string",
    "$defs.canonical_bs.rows[].leaf_ids": "array",
    "$defs.canonical_bs.rows[].leaf_ids[]": "string",
    "$defs.canonical_bs.rows[].opening": "null|number",
    "$defs.canonical_bs.rows[].reconciliation_delta": "number",
    "$defs.canonical_bs.rows[].reconciliation_note": "string",
    "$defs.canonical_bs.rows[].section": "string",
    "$defs.canonical_bs.rows[].synthetic": "boolean",
    "$defs.canonical_bs.schema": "const:bs_v2",
    "$defs.canonical_bs.sections": "array",
    "$defs.canonical_bs.sections[]": "object",
    "$defs.canonical_bs.sections[].id": "string",
    "$defs.canonical_bs.sections[].subtotal": "number",
    "$defs.canonical_bs.source_anchor": "object",
    "$defs.canonical_bs.source_anchor.anchor_status": "enum:DIVERGED,MATCHED,NO_ANCHOR",
    "$defs.canonical_bs.source_anchor.pairs": "object",
    "$defs.canonical_bs.source_anchor.source_balanced": "boolean|null",
    "$defs.canonical_bs.source_anchor.totals_row_found": "boolean",
    "$defs.canonical_bs.status": "enum:BALANCED,MATERIAL_IMBALANCE,MINOR_DRIFT,RECONCILED",
    "$defs.canonical_bs.status_presentation": "ref:status_presentation",
    "$defs.canonical_bs.totals": "object",
    "$defs.canonical_bs.totals.assets": "number",
    "$defs.canonical_bs.totals.current_assets": "number",
    "$defs.canonical_bs.totals.current_liabilities": "number",
    "$defs.canonical_bs.totals.equity": "number",
    "$defs.canonical_bs.totals.equity_plus_liabilities": "number",
    "$defs.canonical_bs.totals.liabilities": "number",
    "$defs.canonical_bs.unmapped": "array",
    "$defs.consensus": "object",
    "$defs.consensus.atoms_compared": "integer",
    "$defs.consensus.consensus_pct": "number",
    "$defs.consensus.disagreements": "array",
    "$defs.consensus.disagreements[]": "object",
    "$defs.consensus.disagreements[].classic_cents": "integer",
    "$defs.consensus.disagreements[].code": "null|string",
    "$defs.consensus.disagreements[].field": "null|string",
    "$defs.consensus.disagreements[].mapped_cents": "integer",
    "$defs.consensus.disagreements[].name": "null|string",
    "$defs.consensus.disagreements[].source_ref": "null|object",
    "$defs.consensus.eligible_balanced": "boolean",
    "$defs.consensus.framings": "null|object",
    "$defs.consensus.legs": "array",
    "$defs.consensus.legs[]": "object",
    "$defs.consensus.legs[].leg": "string",
    "$defs.consensus.legs[].pass": "boolean|null",
    "$defs.consensus.mode": "enum:classic_vs_mapped,dual_map",
    "$defs.consensus.needs_review": "array|null",
    "$defs.consensus.schema": "const:consensus_v1",
    "$defs.consensus.structural": "object",
    "$defs.consensus.structural.aligned": "boolean",
    "$defs.consensus.structural.row_count_a": "integer",
    "$defs.consensus.structural.row_count_b": "integer",
    "$defs.consensus.totals_match": "enum:DIVERGED,MATCHED,NO_ANCHOR",
    "$defs.reconciliation_receipt": "null|object",
    "$defs.reconciliation_receipt.amount_cents": "integer",
    "$defs.reconciliation_receipt.applied_at": "null|string",
    "$defs.reconciliation_receipt.applied_by": "null|string",
    "$defs.reconciliation_receipt.applied_delta": "number",
    "$defs.reconciliation_receipt.content_hash": "string",
    "$defs.reconciliation_receipt.diagnosis_code": "null|string",
    "$defs.reconciliation_receipt.mapping_version": "string",
    "$defs.reconciliation_receipt.model": "null|string",
    "$defs.reconciliation_receipt.origin": "enum:deterministic,llm_proposed",
    "$defs.reconciliation_receipt.original_difference": "number",
    "$defs.reconciliation_receipt.parser_version": "string",
    "$defs.reconciliation_receipt.placement": "enum:balance_sheet,pnl",
    "$defs.reconciliation_receipt.placement_detail": "enum:bs,pl_other_expense,pl_other_income",
    "$defs.reconciliation_receipt.prompt_version": "null|string",
    "$defs.reconciliation_receipt.rationale": "null|string",
    "$defs.reconciliation_receipt.reversible": "boolean",
    "$defs.reconciliation_receipt.target_row_id": "null|string",
    "$defs.status_presentation": "object",
    "$defs.status_presentation.display_en": "string",
    "$defs.status_presentation.display_key": "string",
    "$defs.status_presentation.display_ro": "string",
    "$defs.status_presentation.machine": "enum:BALANCED,MATERIAL_IMBALANCE,MINOR_DRIFT,RECONCILED",
    "$defs.status_presentation.micro_caption": "null|string",
    "$defs.status_presentation.trust_disclosure": "null|object",
    "$defs.status_presentation.trust_disclosure.en": "string",
    "$defs.status_presentation.trust_disclosure.key": "string",
    "$defs.status_presentation.trust_disclosure.ro": "string"
  }
}
