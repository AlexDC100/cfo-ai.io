# GENERATED by scripts/port_hu_pack.py — DO NOT EDIT BY HAND.
# 1:1 mechanical port of the AI-lane classify prompt data at the
# Phase-4 cutover (engine.country_packs.hu_hungary.classification_map
# + the jurisdiction/vocabulary blocks of engine.ai_lane.classify).
# Regenerate: python scripts/port_hu_pack.py   Drift check: --check

schema_version: pack1
jurisdiction: HU
pack_id: actc2000
version: v1
# Act C of 2000 on Accounting (2000. évi C. törvény a számvitelről)
# entered into force on 2001-01-01; no successor pack, window open.
effective_from: 2001-01-01
effective_to: null
legal_sources:
  - citation: "2000. évi C. törvény a számvitelről (Act C of 2000 on Accounting), hatályos 2001. január 1-jétől — in force from 1 January 2001"
  # The uniform chart-of-accounts frame (egységes számlakeret) is the
  # general legal source of the class structure the prompt_guidance
  # class_map encodes; the in-code map carried the Act citation
  # generally (no per-rule section citations), so rules cite nothing
  # finer — honest, not fabricated detail.
  - citation: "Act C of 2000, §§ 160–161: az egységes számlakeret és a számlarend — the uniform chart-of-accounts frame (classes 1–4 balance sheet, 5 costs by nature, 6–7 optional management accounting, 8 expenses, 9 revenues, 0 off-balance memo)"
changelog:
  - version: v1
    date: "2026-08-20"
    notes: "1:1 mechanical port of engine.country_packs.hu_hungary.classification_map at the Phase-4 cutover: HU_CLASS_MAP (10 account classes, Act C of 2000 számlatükör structure) rendered as prompt_guidance.class_map, HU_NOTABLE_ACCOUNTS (21 accounts) as exact rules (gloss verbatim as description; line_id pins the canonical statement line the gloss names), the classify_prompt_block frame text as prompt_guidance header/footer, + the canonical statement-line vocabulary (engine.canonical BS_BUCKETS/PL_BUCKETS) as the statement map, + the D0-D9 diagnosis configuration and auto-reconcile constants (jurisdiction-neutral engine machinery that runs on AI-lane envelopes — mirrored from the same live constants the RO pack mirrors). Generated by scripts/port_hu_pack.py; regeneration must stay byte-identical (--check). The AI-lane classify prompt renders from this pack, and the classify prompt_version derives from its content hash — this exact v1 content aliases to the frozen 'classify_v1' (engine.ai_lane.config), so stored envelopes and the golden corpus stay byte-stable; ANY content change re-versions the prompt and invalidates the AI cache."
