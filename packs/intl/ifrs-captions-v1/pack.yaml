# GENERATED by scripts/port_hu_pack.py — DO NOT EDIT BY HAND.
# 1:1 mechanical port of the AI-lane classify prompt data at the
# Phase-4 cutover (engine.country_packs.hu_hungary.classification_map
# + the jurisdiction/vocabulary blocks of engine.ai_lane.classify).
# Regenerate: python scripts/port_hu_pack.py   Drift check: --check

schema_version: pack1
jurisdiction: INTL
pack_id: ifrs-captions
version: v1
# IFRS caption guidance (IAS 1 / IAS 7). Window opens with EU-wide
# IFRS adoption (Regulation (EC) No 1606/2002 — IAS/IFRS apply from
# financial years starting in 2005). The AI lane resolves the LATEST
# window (no period_end threading), so the start date only gates
# hypothetical effective-dated resolves.
effective_from: 2005-01-01
effective_to: null
legal_sources:
  - citation: "IAS 1 Presentation of Financial Statements (IASB) — the statement caption vocabulary the classify guidance names"
  - citation: "IAS 7 Statement of Cash Flows (IASB)"
changelog:
  - version: v1
    date: "2026-08-20"
    notes: "1:1 mechanical port of engine.ai_lane.classify's international (IFRS-style) prompt data at the Phase-4 cutover: the one-paragraph jurisdiction guidance as prompt_guidance.header (verbatim) and the canonical statement-line vocabulary (engine.canonical BS_BUCKETS/PL_BUCKETS — the IFRS-caption list the classify prompt offers) as the statement map. No account-code rules: IFRS documents carry arbitrary code schemes, classification is by account-name meaning (LLM), memoizable per document family via a confirmed_mappings.yaml overlay. checks/reconcile mirror the same jurisdiction-neutral engine constants the RO and HU packs mirror. Generated by scripts/port_hu_pack.py; regeneration must stay byte-identical (--check). The classify prompt_version derives from this pack's content hash — this exact v1 content aliases to the frozen 'classify_v1' (engine.ai_lane.config); ANY content change re-versions the prompt and invalidates the AI cache."
